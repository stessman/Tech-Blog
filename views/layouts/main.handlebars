

<head>
  <meta charset="UTF-8">
  <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" type='text/css' href="./css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/luxon@1.26.0/build/global/luxon.min.js"></script>
</head>

<body>
    <header class="header">
        <nav class="navbar navbar-expand-lg myNav navbar-static-top">
            <a class="navbar-brand m-5 navTitle" href="#">Tech Blog</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto m-5">
                    <li class="nav-item active">
                        <a class="nav-link m-2" href="/">Home</a>
                    </li>
                    <li class="nav-item m-2">
                        <a class="nav-link" href="/dashboardpage">Dashboard</a>
                    </li>
                    {{#if loggedIn}}
                    <li class="nav-item m-2">
                        <a class="nav-link" id="logout_button">Logout</a>
                    </li>
                    {{else}}
                    <li class="nav-item m-2">
                        <a class="nav-link" href="/loginpage">Login</a>
                    </li>
                    {{/if}}
                </ul>
            </div>
        </nav>
    </header>
    <main>
        {{{ body }}}
    </main>
    {{#if loggedIn}}
    <script src="./js/logout.js"></script>
    {{/if}}
  </body>

</html>